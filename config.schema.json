{
  "pluginAlias": "UniversalGdo",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
    "serverIPAddress":{
      "type":"string",
      "title": "Server IP Address",
      "required":true,
      "description": "Base path of your server. For a local device an IP address is sufficient"
    },
      "doorOneName": {
        "title": "Door 1 Name",
        "type": "string",
        "required": true,
        "description": "Set the Door name for display in the Home app."
      },
      "doorTwoName": {
        "type": "string",
        "title": "Door 2 Name",
        "required": true,
        "description": "Set the Door name for display in the Home app."
      }
      }
    }
}